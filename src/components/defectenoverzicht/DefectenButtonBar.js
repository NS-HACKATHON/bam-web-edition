import * as React from "react";

export const DefectenButtonBarComponent = props => {
  return (
    <div>
      <button>Invoeren</button>
      <button>Wijzigen</button>
      <button>Gereed</button>
    </div>
  );
};
